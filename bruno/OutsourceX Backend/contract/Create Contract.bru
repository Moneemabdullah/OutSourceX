meta {
  name: Create Contract
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/contracts
  body: json
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{accessToken}}
}

body:json {
  {
    "proposalID": "replace-proposal-id",
    "title": "Backend build contract",
    "description": "Create a complete backend implementation"
  }
}
